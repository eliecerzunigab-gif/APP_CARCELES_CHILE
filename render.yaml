services:
  - type: web
    name: app-carceles-chile
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node src/server.js
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PORT
        value: 10000
